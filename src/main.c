#include <stdio.h>
#include <stdlib.h>
#include <conio.h>
#include "menu.h"

/* run this program using the console pauser or add your own getch, system("pause") or input loop */
void menu_main_jump(void);

int main(int argc, char *argv[]) {
	system("cls");
	index_menu();

	return 0;
}